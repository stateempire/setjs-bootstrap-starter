name: SetJS post-css starter kit
version: 1.0.0
aws_vars: export $(cat .aws-vars | xargs)
loader: progress-bar
local:
  environment: local
  site_port: 60003
development:
  environment: development
master:
  environment: production
